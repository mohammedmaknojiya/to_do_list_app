import React from 'react';

function TaskList({list}) {
        // console.log(list)
        // console.log(list.id,list.message)
        //const {id,message}={list};
        //console.log(id);
        return(
            <div>
                <div>
        {/* I am with id {id} and with message {message} */}
                </div>
                <button>Done!!</button>
            </div>
        )
    }    
export default TaskList;